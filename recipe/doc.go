// Package recipe defines the canonical recipe format: types, YAML/JSON
// (un)marshalling, and validation. A recipe declaratively describes a
// pipeline of gseq-table operations against a tabular source.
package recipe
